10 REM Test OR operator
20 LET A = 1
30 LET B = 0
40 IF A OR B THEN PRINT "AT LEAST ONE TRUE"
50 LET A = 0
60 IF A OR B THEN PRINT "AT LEAST ONE TRUE (SKIPPED)" : GOTO 80
70 PRINT "BOTH FALSE"
80 END
